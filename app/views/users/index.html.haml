%h1 Users

%table
  %thead
    %tr
      %th Email
      %th Status
      %th Actions

  %tbody
    - @users.each do |user|
      %tr
        %td= user.email
        %td= user.approved ? 'Approved' : 'Pending'
        %td
          - unless user.approved
            = link_to 'Approve', approve_user_path(user.id)
            |
          = link_to 'Delete', user_path(user.id), method: :delete
